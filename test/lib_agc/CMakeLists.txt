if(USE_CUSTOM_CMAKE)
    add_subdirectory(test_process_frame)
else()
    cmake_minimum_required(VERSION 3.21)
    include($ENV{XMOS_CMAKE_PATH}/xcommon.cmake)
    project(fwk_voice_tests_agc)
    add_subdirectory(test_agc_xccm)
endif()
